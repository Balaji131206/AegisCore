# MultiThreadSystemJAVA Documentation Site

This directory contains a Docusaurus site that uses the existing `docs/` folder as the documentation source.

## Quick start

```bash
cd website
npm install
npm start
```

Then open `http://localhost:3000`.

## Build

```bash
cd website
npm run build
```
